import React from 'react';

const Results = function (props) {
  const { cards } = props;
  return (
    <section id="results">
      <h2>About your results:</h2>
    </section>
  );
};

export default Results;
